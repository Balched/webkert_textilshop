import { AppPasswordToggleDirective } from './app-password-toggle.directive';

describe('AppPasswordToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPasswordToggleDirective();
    expect(directive).toBeTruthy();
  });
});
