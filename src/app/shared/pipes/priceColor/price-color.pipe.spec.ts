import { PriceColorPipe } from './price-color.pipe';

describe('PriceColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceColorPipe();
    expect(pipe).toBeTruthy();
  });
});
